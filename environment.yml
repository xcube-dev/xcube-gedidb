name: xcube-gedidb-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Required
  - python>=3.10
  - xarray
  - xcube
  - pip
  - gdal
  - pip:
      - gedidb
  # for testing
  - black
  - flake8
  - pytest
  - pytest-cov
  - pytest-recording
